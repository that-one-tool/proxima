{
	"name": "@that-one-tool/proxima-resp",
	"version": "0.1.0",
	"description": "A minimalist TCP reverse proxy for redis-based server",
	"main": "dist/index.js",
	"scripts": {
		"build": "rm -rf dist && node esbuild.config.mjs",
		"check-types": "tsc --noEmit",
		"clear:build": "rm -rf dist && rm -rf .cache",
		"clear:turbo": "rm -rf .turbo",
		"start:dev": "npm run build && node --env-file=.env dist/index.js",
		"lint": "eslint . --max-warnings 0",
		"test": "echo \"Error: no test specified\" && exit 1"
	},
	"author": "Jason Mejane",
	"license": "MIT",
	"keywords": [
		"tcp",
		"redis",
		"proxy",
		"tcp-proxy",
		"redis-proxy",
		"server"
	],
	"dependencies": {
		"@that-one-tool/proxima-core": "*"
	},
	"devDependencies": {
		"@repo/eslint": "*",
		"@repo/typescript": "*",
		"@types/jest": "^29.5.14",
		"@types/node": "^22.13.14",
		"esbuild": "^0.25.1",
		"jest": "^29.7.0",
		"ts-jest": "^29.3.0",
		"typescript": "^5.7.2"
	},
	"jest": {
		"moduleFileExtensions": [
			"js",
			"json",
			"ts"
		],
		"rootDir": "src",
		"testRegex": ".*\\.spec\\.ts$",
		"transform": {
			"^.+\\.(t|j)s$": [
				"ts-jest",
				{
					"isolatedModules": true
				}
			]
		},
		"collectCoverageFrom": [
			"**/*.(t|j)s"
		],
		"coverageDirectory": "../coverage",
		"testEnvironment": "node"
	}
}
